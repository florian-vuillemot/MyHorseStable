<h2>Edit osteopath</h2>

<%= render "form.html", changeset: @changeset,
                        action: osteopath_path(@conn, :update, @osteopath) %>

<%= link "Back", to: osteopath_path(@conn, :index) %>
